/**
 * Reference values from `schema.sql` seed data for `spec_status.code`.
 */
export enum SpecStatusCode {
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED',
  FAILED = 'FAILED',
  REVIEWED = 'REVIEWED',
}
